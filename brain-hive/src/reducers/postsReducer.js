
const INITIAL_STATE = {
    //list initial posts
    list: [],
}

export default (state = INITIAL_STATE, action) => {
    //create switch statement on whatever the action is
    switch (action, type) {
        default: 
        return state; 
    }
}